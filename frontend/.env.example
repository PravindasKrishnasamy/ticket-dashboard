# Base URL of the backend API (Step 2). Example for local dev:
# REACT_APP_API_URL=http://localhost:4000/api
# When deployed on EKS, this will point at the backend Service/Ingress instead.
REACT_APP_API_URL=http://localhost:4000/api
